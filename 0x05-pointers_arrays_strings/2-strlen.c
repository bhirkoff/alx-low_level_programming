#include "main.h"

/**
 * _strlen - A function that returns the length of a string
 *
 * @s: pointer passed for the char value
 *
 * Return: 0
 */

int _strlen(char *s)
{
	_putchar("%d", strlen(s));
}
